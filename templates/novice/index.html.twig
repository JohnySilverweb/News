{% extends 'base.html.twig' %}
{% block body_class %}form_page{% endblock %}

{% block body %}

<div class="form_title">
    <h1>Add some news</h1>
</div>
{{ form(form) }}
<hr>
<div class="buttons">
   <button><a href="{{ path('novice_table') }}">Tables</a></button>
   <button><a href="{{ path('novice_search') }}">Search news</a></button>
   <button><a href="{{ path('novice_cards') }}">Cards</a></button>
</div>

 
{% endblock %}